Beta-1.2.0

Features:
- added settings menu before starting a race, with options for fuel, starting speed and generation seed, which also shows preview of the star system
- win screen now shows time at passing last gate

Fixes:
- fixed camera zoom, also added zoom out limit
- rewind hint after crashing now scaled correctly for possibly changed input key
- "Exit" button in settings now actually reverts to previous settings